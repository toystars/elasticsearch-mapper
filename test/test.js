'use strict';

var expect = require('chai').expect;
var mapper = require('../index');


describe('#ES-JS', function() {
  it('should pass dummy test', function() {
    var number = 10;
    expect(number).to.equal(10);
  });
});
